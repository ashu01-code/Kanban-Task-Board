# Kanban-Task-Board
Kanban Task Board is a React.js and Vite task management app. Users can add, edit, delete, search, prioritize, and move tasks across To Do, In Progress, and Done columns. It uses React state and props for dynamic updates, while localStorage saves tasks and keeps data persistent even after browser refresh.
